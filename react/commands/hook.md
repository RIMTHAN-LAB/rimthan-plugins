Create a custom React hook following team conventions.

Please generate:

1. Custom hook with TypeScript
2. Proper typing for parameters and return value
3. Unit tests using @testing-library/react-hooks
4. JSDoc documentation
5. Usage example

Hook specifications:
- Name: {{name}}
- Location: src/hooks/{{name}}.ts
- Include dependency array optimization
- Handle cleanup if needed
- Add proper error handling
- Include loading and error states if applicable

The hook should follow React hooks rules and naming conventions (start with 'use').
