#!/bin/bash

# Check if test script exists in package.json
if npm run 2>/dev/null | grep -q "^  test$"; then
  # Run tests
  if npm test; then
    echo "✅ Tests passed"
  else
    echo "❌ Tests failed"
    exit 1
  fi
else
  echo "⚠️  No tests defined, continuing..."
fi
